export type MovieObject = {
  imdbID: string;
  Title: string;
  Year: number;
  Genre: string;
  Plot: string;
  Type: string;
  Released: string;
  Poster: string;
  Rating?: number; // Store rating value (optional)
};
